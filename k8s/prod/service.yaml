apiVersion: v1
kind: Service
metadata:
  name: multi-env-prod-service
  namespace: prod
spec:
  type: NodePort
  selector:
    app: multi-env-prod
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30082