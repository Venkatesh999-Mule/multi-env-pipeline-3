apiVersion: v1
kind: Service
metadata:
  name: multi-env-dev-service
  namespace: dev
spec:
  type: NodePort
  selector:
    app: multi-env-dev
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30081